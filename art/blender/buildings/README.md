# Building source status

`foundation_wood.blend` is a real Blender 4.5.13 LTS source: 10 planks, 7 rim/joist
beams, editable bevels/UVs, one packed Cycles-baked wood material. Validated and
exported/rendered; **user visual acceptance pending**. See its adjacent provenance
record and `docs/BLENDER_FOUNDATION_PILOT.md`. Do not rerun the empty-template or
one-shot recipe over authored work. Wall and campfire below remain pending.

- `foundation_wood.blend`: 2 × 2 × 0.2 m (Blender XYZ); wooden boards, visible support structure, bevels and restrained wear. Preserve exact module footprint.
- `wall_wood.blend`: 2 × 0.18 × 2.4 m; boards with grain, readable joints and supports, both sides finished. Preserve snap dimensions.
- `campfire_basic.blend`: within 1.1 × 1.1 × 0.5 m; irregular stone ring and crossed logs with char variation. **No flame geometry or light in EXPORT**; gameplay owns fire.

Not simply default cubes/cylinders. Remaining sources require Blender modeling,
UVs, materials and visual review, in separately authorized Issues. See `docs/ART_PIPELINE.md`.
